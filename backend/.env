# FILE: backend/.env
APP_ENV=development
DEBUG=True

# Connect through SSH tunnel (local forwards to VPS 5432 → local 5433)
# OR directly if your VPS Postgres accepts remote connections
DATABASE_URL=postgresql+asyncpg://rich:reddpos@127.0.0.1:5433/crypto_charts_dev

JWT_SECRET=dev-secret-key
CORS_ORIGINS=http://127.0.0.1:5173
